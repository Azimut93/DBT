version: 2

sources:
  - name: raw
    description: "Raw e-commerce data loaded from source systems"
    database: DEMO
    schema: RAW

    tables:
      - name: raw_customers
        description: "Raw customers data from Shopify"

      - name: raw_products
        description: "Raw products catalog"

      - name: raw_orders
        description: "Raw orders data"

      - name: raw_order_items
        description: "Raw order line items"

      - name: raw_payments
        description: "Raw payments data"
